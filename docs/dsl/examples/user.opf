package cloud.orbit.examples.dsl;

grain User {
    enum LoginResult {
        OK = 0,
        DENIED = 1
    }

    struct LoginRequest {
        String username = 0;
        String password = 1;
    }

    LoginResult login(LoginRequest request = 0);
}